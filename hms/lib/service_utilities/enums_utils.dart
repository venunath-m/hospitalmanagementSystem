enum FieldType {
  text,
  multiline,
  email,
  phone,
  password,
  zipcode,
  number,
  currency,
  url,
  date,
  datetime,
}

enum ToastType { success, info, warning, error }

enum AppThemeMode { system, light, dark, blue, green, purple }
